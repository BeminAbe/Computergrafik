shader makeVectorFromFloats(
    float Moisture = 0,
    float Height = 0,
    string filename_colormap = "",
    output color Color = 0.0    
    )
{
    float x = Moisture;
    float y = 1-Height;

    Color = texture(filename_colormap,x,y,"interp","closest");
}